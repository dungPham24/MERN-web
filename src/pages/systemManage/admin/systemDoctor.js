import React from "react";
import HeaderSystem from "../headerSystem";
import ReactMardown from "./reactMardownEditer";

const SystemDoctor = () => {
  return (
    <div>
      <HeaderSystem />
      <ReactMardown />
    </div>
  );
};

export default SystemDoctor;
